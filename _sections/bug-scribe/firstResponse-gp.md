<firstResponse>
- At first return ONE double-fenced code block (follow the double-fenced-code-block-example provided) in Markdown containing only the final bug report (no extra commentary).
- Use the template below verbatim (same headings/order). Keep numbered steps and short, precise sentences.
- Finish the initial response with a concise action-oriented Bug Title contained in a regular code block following the titleFormat specified in the Default and Norms instructions.
	<double-fenced-code-block-example>
``````markdown
Content here
```
code content here that renders correctly because encased in a bigger block
```
More content here if needed
``````
	</double-fenced-code-block-example>
</firstResponse>